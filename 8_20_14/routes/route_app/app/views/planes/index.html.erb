<h1>Hello, pilots!</h1>

<% @planes.each do |plane| %>
  <div>
    <h2><%= plane.name %></h2>
    <p><%= plane.kind %></p>
    <p><%= plane.description %></p>
    <a href="/planes/<%= plane.id %>">Show</a>
  </div>
<% end %>